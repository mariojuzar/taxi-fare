# Taxi Fare

## Requirements

- Go installed

## Assumption

- On the paper not stated when the program stop, so I assume that the input for the program stop when the input send `finished`

## Running program

you can run program using command
```
make run
```

or if you want to build binary first and then run it, you can use this command
```
make build-run
```